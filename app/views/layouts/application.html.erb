<!DOCTYPE html>
<html lang="en" class="h-100">
  <head>
    <title>Bayesian Predictions</title>
    <!-- Required meta tags -->
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>

    <!-- Rails Imports -->
      <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= javascript_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= favicon_pack_tag 'favicon.ico', type: 'image/icon' %>
    <!-- Math Setups -->
      <script script>
          MathJax = {
            tex: {
              inlineMath: [ ['$','$'], ["\\(","\\)"] ],
              processEscapes: true
            },
            svg: {
              fontCache: 'global',
            }
          };
      </script>
      <script type="text/javascript" id="MathJax-script" src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js"></script>

  </head>

  <body id="appBody" class="container-lg bg-dark text-white">
      <header class="d-flex justify-content-between my-2">

          <% if user_signed_in? %>
            <a href="/" class="btn text-white btn-secondary">Bayesian Predictions</a>
            <nav class="nav align-content-center">
            <li class="d-none d-sm-block nav-item mx-2">
              <%= link_to "#{current_user.email}", "/profile/#{current_user.id}", class: "btn btn-secondary text-center text-white nav-link" %>
            </li>
            <li class="nav-item mx-2">
              <%= link_to "Logout", destroy_user_session_path, method: "delete", class: "btn btn-secondary text-white nav-link" %>
            </li>
          <% else %>
            <a href="/" class="d-none d-sm-block btn text-white btn-secondary">Bayesian Predictions</a>
            <nav class="nav align-content-center">
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "text-white btn btn-info nav-link" %>
            </li>
            <li class="nav-item mx-2">
              <%= link_to "Sign Up", new_user_registration_path, class: "text-white btn btn-success nav-link" %>
            </li>
          <% end %>
        </nav>
      </header>
      <main>
        <%= yield %>
      </main>
      <footer class="mt-5 d-flex justify-content-center">
        <p>Copyright © 2021 <a class="text-decoration-none text-white" target="_blank" rel="noopener noreferrer" href="https://www.linkedin.com/in/jan-goergens">Jan Görgens</a></p>
      </footer>
  </body>
</html>
